"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sdkInit = sdkInit;
const core_1 = require("@sentry/core");
const NativeLogListener_1 = require("./NativeLogListener");
const optionsUtils_1 = require("./utils/optionsUtils");
const version_1 = require("./version");
const wrapper_1 = require("./wrapper");
/**
 * Post setup the Capacitor client
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
function PostSetupCapacitorClient() {
    var _a, _b;
    const client = (0, core_1.getClient)();
    if (client) {
        const options = client.getOptions();
        if (!((_a = options._metadata) === null || _a === void 0 ? void 0 : _a.sdk)) {
            return;
        }
        const sdk = options._metadata.sdk;
        sdk.name = `${sdk.name}.capacitor`;
        sdk.version = version_1.SDK_VERSION;
        (_b = sdk.packages) === null || _b === void 0 ? void 0 : _b.push({
            name: version_1.SDK_NAME,
            version: version_1.SDK_VERSION
        });
        options._metadata.sdk = sdk;
    }
}
/**
 * Initializes the Sibling and Native Sentry SDKs.
 * @param browserOptions - The browser options to use for the sibling SDK.
 * @param nativeOptions - The native options to use for the native SDK.
 * @param originalInit - The original init function to use for the sibling SDK.
 * @param customTransport - The custom transport to use.
 */
function sdkInit(browserOptions, nativeOptions, originalInit, customTransport) {
    // We first initialize the NATIVE SDK to avoid the Javascript SDK to invoke any
    // feature from the NATIVE SDK without the options being set.
    // eslint-disable-next-line @typescript-eslint/no-floating-promises
    wrapper_1.NATIVE.initNativeSdk(nativeOptions)
        .then(() => {
        var _a;
        if (nativeOptions.debug) {
            const logHandler = (_a = nativeOptions.onNativeLog) !== null && _a !== void 0 ? _a : NativeLogListener_1.defaultNativeLogHandler;
            (0, NativeLogListener_1.setupNativeLogListener)(logHandler);
        }
        originalInit(browserOptions);
    }).catch((error) => {
        // Fallback to JavaScript only SDK Init.
        // eslint-disable-next-line no-console
        console.error('Native Sentry SDK failed to initialize. Using Sentry JavaScript SDK without native integraiton.\n', { error });
        (0, optionsUtils_1.RestoreNonNativeOptions)(browserOptions, customTransport);
        originalInit(browserOptions);
    }).finally(() => {
        PostSetupCapacitorClient();
    });
}
//# sourceMappingURL=client.js.map