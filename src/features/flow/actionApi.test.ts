import { beforeEach,describe,expect,it,vi } from 'vitest';const mocks=vi.hoisted(()=>({apiRequest:vi.fn()}));vi.mock('../../core/api/client',()=>({apiRequest:mocks.apiRequest}));import { recordActionLifecycle } from './actionApi';
describe('Phase 20 Action lifecycle API',()=>{beforeEach(()=>mocks.apiRequest.mockReset().mockResolvedValue({ok:true}));it('records the workflow result on the linked Action endpoint',async()=>{await recordActionLifecycle('ws/1','action/7','complete','TX-14');expect(mocks.apiRequest).toHaveBeenCalledWith('api/mobile/v1/workspaces/ws%2F1/actions/action%2F7/source-event',expect.objectContaining({method:'POST',payload:{event:'complete',detail:'TX-14'}}));});});
