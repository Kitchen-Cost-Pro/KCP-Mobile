import { beforeEach, describe, expect, it, vi } from 'vitest';
const mocks=vi.hoisted(()=>({apiRequest:vi.fn()}));vi.mock('../../core/api/client',()=>({apiRequest:mocks.apiRequest}));
import { completeTask, loadTask, loadTasks, saveTaskProgress } from './taskApi';
describe('KCP Flow Action API',()=>{beforeEach(()=>mocks.apiRequest.mockReset().mockResolvedValue({ok:true}));it('uses versioned, location-scoped Action routes',async()=>{await loadTasks('ws/one','loc-1');await loadTask('ws/one','action/1');const progress={revision:2,notes:'Done',steps:[]};await saveTaskProgress('ws/one','action/1',progress);await completeTask('ws/one','action/1',progress);expect(mocks.apiRequest).toHaveBeenNthCalledWith(1,'api/mobile/v1/workspaces/ws%2Fone/actions',expect.objectContaining({query:{locationId:'loc-1'}}));expect(mocks.apiRequest).toHaveBeenNthCalledWith(2,'api/mobile/v1/workspaces/ws%2Fone/actions/action%2F1');expect(mocks.apiRequest).toHaveBeenNthCalledWith(3,'api/mobile/v1/workspaces/ws%2Fone/actions/action%2F1/progress',expect.objectContaining({method:'PUT'}));expect(mocks.apiRequest).toHaveBeenNthCalledWith(4,'api/mobile/v1/workspaces/ws%2Fone/actions/action%2F1/complete',expect.objectContaining({method:'POST',payload:expect.objectContaining({confirm:true})}));});});
